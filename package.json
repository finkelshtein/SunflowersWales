{
  "name": "sunflowers-wales-website",
  "version": "1.0.0",
  "private": true,
  "description": "Sunflowers Wales — HugoBlox site",
  "scripts": {
    "dev": "hugo && npm run index && hugo server --disableFastRender",
    "build": "hugo --minify && npm run index",
    "index": "pagefind --site public"
  },
  "dependencies": {
    "@tailwindcss/cli": "^4.1.12",
    "@tailwindcss/typography": "^0.5.10",
    "preact": "^10.27.2",
    "tailwindcss": "^4.1.12"
  },
  "allowScripts": {
    "@parcel/watcher@2.5.1": true
  },
  "devDependencies": {
    "pagefind": "^1.5.2"
  }
}
